language: node_js
node_js:
  - "6"
before_script:
  - npm install gitbook-cli
  - npm install gitbook-publish@latest
script: 
  - gitpub
  - git push "https://${GITHUB_TOKEN}@github.com/${GITHUB_REPO}.git" gh-pages:gh-pages
